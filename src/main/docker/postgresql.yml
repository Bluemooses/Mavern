version: '2'
services:
  mavern-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Mavern/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Mavern
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
